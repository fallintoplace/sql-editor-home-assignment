// The unauthenticated header-forwarding demo has been retired.
// npm start now delegates to the bounded, server-owned workbench API.
console.error('Use npm run setup, then npm run dev (or npm start) from the repository root.');
process.exitCode = 1;
